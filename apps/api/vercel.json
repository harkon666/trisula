{
    "buildCommand": "cd ../.. && turbo run build --filter=api",
    "outputDirectory": "",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.js"
        }
    ]
}