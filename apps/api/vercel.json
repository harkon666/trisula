{
  "framework": null,
  "buildCommand": "bun run build && mkdir -p public && echo 'API is running' > public/index.html",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.ts"
    }
  ]
}
